# Day 6：精读定时器、日志和基础并发设施

## 今天的任务定义

今天补的是“服务器为什么能长期稳定运行”这部分。

前几天你已经把主流程、并发模型、HTTP、数据库业务都串起来了。今天要把这些辅助但关键的工程板块读透：超时连接怎么清理，日志为什么分同步和异步，并发同步工具到底保护了什么。

---

## 今天必须精读的文件

- `timer/lst_timer.h`
- `timer/lst_timer.cpp`
- `log/log.h`
- `log/log.cpp`
- `log/block_queue.h`
- `lock/locker.h`
- `webserver.cpp`

建议顺序：

1. 先读 `lock/locker.h`
2. 再读 `timer/*`
3. 再读 `log/*`
4. 最后回到 `webserver.cpp` 对照调用链

---

## 第一遍：先把同步工具认全

先把这些封装过一遍：

- `sem`
- `locker`
- `cond`

第一遍结束后，你至少要知道：

- `sem` 用来做资源计数或任务唤醒
- `locker` 用来保护共享数据
- `cond` 用来做条件等待/通知

今天不要求你重写这些类，但必须知道它们在项目里的使用场景。

---

## 第二遍：按板块精读定时器

### 板块 1：定时器数据结构

重点文件：

- `timer/lst_timer.h`
- `timer/lst_timer.cpp`

重点类和结构：

- `util_timer`
- `sort_timer_lst`
- `client_data`

你要看清楚：

- 每个定时器节点保存什么
- 为什么需要 `prev` 和 `next`
- `expire` 字段怎么参与排序
- `cb_func` 回调负责做什么

要达到的水平：

- 能解释这是一条按过期时间排序的双向链表
- 能说明这种结构为什么适合周期性检查最近要过期的连接

建议重点盯的成员：

- `expire`
- `cb_func`
- `user_data`
- `prev`
- `next`

### 板块 2：定时器链表操作

重点函数：

- `sort_timer_lst::add_timer`
- `sort_timer_lst::adjust_timer`
- `sort_timer_lst::del_timer`
- `sort_timer_lst::tick`

你要逐个看：

1. 新定时器怎么插入
2. 活跃连接为什么要调整超时时间
3. 删除定时器怎么处理头结点、尾结点、中间节点
4. `tick()` 为什么从头开始检查就够了

要达到的水平：

- 能说出“为什么只需要不断检查链表头部”
- 能解释复杂度大致落在哪里

建议断点位置：

- `add_timer(...)`
- `adjust_timer(...)`
- `tick()`

### 板块 3：回到 `webserver.cpp` 看定时器怎么接入主流程

重点函数：

- `WebServer::timer`
- `WebServer::adjust_timer`
- `WebServer::deal_timer`
- `WebServer::dealwithsignal`
- `WebServer::eventLoop`

你要看清楚：

- 新连接建立时，在哪里创建定时器
- 读写事件发生时，在哪里刷新定时器
- 连接异常或超时时，在哪里关闭 fd
- `SIGALRM` 到来后，为什么不是直接在信号处理函数里处理超时

你要达到的水平：

- 能完整描述“连接建立 -> 定时器挂上 -> 活跃时刷新 -> 超时后关闭”的全过程

建议断点位置：

- `timer(connfd, client_address);`
- `adjust_timer(timer);`
- `deal_timer(timer, sockfd);`
- `utils.timer_handler();`

---

## 第三遍：按板块精读日志系统

### 板块 1：阻塞队列

重点文件：

- `log/block_queue.h`

重点看：

- 队列内部保存了什么
- 入队和出队怎么同步
- 队列满和队列空时怎么处理

要达到的水平：

- 能说明阻塞队列是异步日志的缓冲层
- 能解释生产者和消费者分别是谁

建议盯的点：

- 队列容量
- 互斥锁
- 条件变量

### 板块 2：日志类结构

重点文件：

- `log/log.h`
- `log/log.cpp`

重点函数：

- `Log::init`
- `Log::write_log`
- `Log::flush`
- `Log::async_write_log`

你要看清楚：

- 日志初始化参数分别控制什么
- 同步和异步模式如何分叉
- 日志文件名怎么组织
- 后台写线程如何持续消费日志

要达到的水平：

- 能讲清同步日志和异步日志的真实区别
- 能说明为什么异步日志对高并发服务器更友好

建议断点位置：

- `Log::init(...)`
- `Log::write_log(...)`
- `Log::async_write_log()`

### 板块 3：回到调用方看日志怎么进入主流程

重点位置：

- `webserver.cpp` 中的 `LOG_INFO` / `LOG_ERROR`
- `http_conn.cpp` 中关键日志位置

你要看清楚：

- 哪些链路会打日志
- 日志是功能辅助，不是业务主流程
- 压测时关闭日志为什么能提高吞吐

要达到的水平：

- 能从性能角度评价日志系统，而不是只从“方便排查问题”这个角度说

---

## 今天必须手写出来的调用链

### 1. 超时连接回收链

`新连接建立 -> WebServer::timer -> 加入定时器链表 -> 连接活跃时 adjust_timer -> SIGALRM -> dealwithsignal -> timer_handler/tick -> cb_func -> close`

### 2. 异步日志链

`业务线程/主线程打日志 -> Log::write_log -> block_queue 入队 -> 后台线程 async_write_log -> 落盘`

### 3. 同步工具使用场景图

至少写清：

- 线程池任务队列用了什么同步工具
- 连接池用了什么同步工具
- 日志阻塞队列用了什么同步工具

---

## 今天适合重点思考的问题

- 为什么定时器这里用排序链表，而不是最小堆或时间轮
- 为什么信号处理函数里不能做复杂清理逻辑
- 日志系统如果队列满了，现在的策略是否合理
- 当前项目里的同步封装是否足够现代，是否有替代方案

---

## 今天结束时的验收标准

你必须做到：

- 能讲清一个连接超时后是怎么被回收的
- 能讲清 `SIGALRM -> 管道 -> epoll -> tick` 这条链路
- 能讲清同步日志和异步日志的实现差异
- 能把锁、信号量、条件变量和实际模块对应起来

---

## 今日输出要求

今天的笔记必须包含：

### 1. 超时连接回收流程图

从建链到回收完整画出来。

### 2. 日志系统结构图

同步和异步都要写。

### 3. 同步工具落点表

写成“模块 -> 使用工具 -> 保护对象/目的”的形式。

### 4. 设计评价

至少写：

- 定时器设计的优点和局限
- 日志设计的优点和局限

